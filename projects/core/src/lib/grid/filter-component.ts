import { CellComponent } from './cell-component';

export class FilterComponent extends CellComponent {
  /**
   * Constructor()
   * @param options
   */
  constructor(options?: Partial<FilterComponent>) {
    super(options);
  }
}
